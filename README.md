# Backend-Software-Engineer-Task
## How to run

- Clone the project repo and ```cd``` to directory
- ``npm install``
- Add ``.env`` file containing your environement variables like ``.example.env``
- Run with ``npm start``
